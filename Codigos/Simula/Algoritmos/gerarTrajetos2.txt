\Inicio{
    \Comentario{trajetos para jovens} \\
    num = numero\_aleatorio\_entre(0, 6) \\
    
    \Comentario{Primeiro trajeto para jovens} \\
    \Se{num $==$ 0} {
          trajeto = lista\_vazia() \\
          adiciona(novo\_local(casa, 10), trajeto) \\
          adiciona(novo\_local(escolheAleatoriamente(locaisEstudo), 55), trajeto) \\
          adiciona(novo\_local(casa, 35), trajeto) \\
          adiciona(trajeto, trajetosJovem) \\
    }
    
    \Comentario{Segundo trajeto para jovens} \\
    \Se{num $==$ 1} {
          trajeto = lista\_vazia() \\
          adiciona(novo\_local(casa, 20), trajeto) \\
          adiciona(novo\_local(escolheAleatoriamente(locaisLazer), 45), trajeto) \\
          adiciona(novo\_local(casa, 35), trajeto) \\
          adiciona(trajeto, trajetosJovem) \\
    }
    
    \Comentario{Terceiro trajeto para jovens} \\
    \Se{num $==$ 2} {
          trajeto = lista\_vazia() \\
          adiciona(novo\_local(casa, 10), trajeto) \\
          adiciona(novo\_local(escolheAleatoriamente(locaisTrabalho), 55), trajeto) \\
          adiciona(novo\_local(casa, 35), trajeto) \\
          adiciona(trajeto, trajetosJovem) \\
    }
    
    \Comentario{Quarto trajeto para jovens} \\
    \Se{num $==$ 3} {
          trajeto = lista\_vazia() \\
          adiciona(novo\_local(casa, 10), trajeto) \\
          adiciona(novo\_local(escolheAleatoriamente(locaisEstudo), 30), trajeto) \\
          adiciona(novo\_local(escolheAleatoriamente(locaisLazer), 25), trajeto) \\
          adiciona(novo\_local(casa, 35), trajeto) \\
          adiciona(trajeto, trajetosJovem) \\
    }
    
    \Comentario{Quinto trajeto para jovens} \\
    \Se{num $==$ 4} {
          trajeto = lista\_vazia() \\
          adiciona(novo\_local(casa, 10), trajeto) \\
          adiciona(novo\_local(escolheAleatoriamente(locaisEstudo), 25), trajeto) \\
          adiciona(novo\_local(escolheAleatoriamente(locaisTrabalho), 30), trajeto) \\
          adiciona(novo\_local(casa, 35), trajeto) \\
          adiciona(trajeto, trajetosJovem) \\
    }
    
    \Comentario{Sexto trajeto para jovens} \\
    \Se{num $==$ 5} {
          trajeto = lista\_vazia() \\
          adiciona(novo\_local(casa, 10), trajeto) \\
          adiciona(novo\_local(escolheAleatoriamente(locaisEstudo), 20), trajeto) \\
          adiciona(novo\_local(escolheAleatoriamente(locaisTrabalho), 25), trajeto) \\
          adiciona(novo\_local(escolheAleatoriamente(locaisLazer), 10), trajeto) \\
          adiciona(novo\_local(casa, 35), trajeto) \\
          adiciona(trajeto, trajetosJovem) \\
    }
}
