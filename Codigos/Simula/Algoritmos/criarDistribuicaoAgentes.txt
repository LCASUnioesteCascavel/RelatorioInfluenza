\Inicio{
  \Comentario{Lê arquivo que contém todos os casos} \\

  \Comentario{Agrupa casos por dia de ocorrência} \\

  \Comentario{Para cada dia mantém um percentual dos casos} \\
  
  \Comentario{Agrupa casos por faixa etária} \\
  
  \Comentario{Atribui quantidades de masculinos e femininos de acordo com as taxas} \\

  \Comentario{Ordena os casos por dia de ocorrência} \\
  
  \Comentario{Para cada caso substitui o ponto original pelo ponto mais próximo no ambiente} \\

  \Comentario{Escrita do arquivo DistribuicaoAgentes.csv} \\
	arquivo = abreArquivo("DistribuicaoAgentes.csv") \\
  
  \Comentario{Escrita da distribuição dos agentes} \\
	\Para{cada caso $c$ que será inserido durante a simulação} {
				escreve(quadra($c$), arquivo) \\
	      escreve(";", arquivo) \\
				escreve(lote($c$), arquivo) \\
	      escreve(";", arquivo) \\	      
				escreve(x($c$), arquivo) \\
	      escreve(";", arquivo) \\
				escreve(y($c$), arquivo) \\
	      escreve(";", arquivo) \\
				escreve(sexo($c$), arquivo) \\
	      escreve(";", arquivo) \\
				escreve(faixaEtaria($c$), arquivo) \\
	      escreve(";", arquivo) \\
				escreve("I", arquivo) \\
	      escreve(";", arquivo) \\
				escreve("2", arquivo) \\
        escreve(";", arquivo) \\
				escreve(dia($c$), arquivo) \\
				escreve(novaLinha, arquivo) \\
	}
	fechaArquivo(arquivo) \\
}
