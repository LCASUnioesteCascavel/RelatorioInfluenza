\Inicio{

  \Comentario{Criação das rotas para adultos} \\
	rotasAdulto = lista\_vazia() \\
	k = 0 \\
	\Para{$i$ de $0$ ate tamanho(trajetosAdulto)} {
	      \Para{$j$ de $0$ ate tamanho(trajetosAdulto[$i$]) $-1$} {
              rota = lista\_vazia() \\
              localOrigem = trajetosAdulto[$i$, $j$] \\
              localDestino = trajetosAdulto[$i$, $j+1$] \\
              quadraOrigem = indicesQuadras[localOrigem.quadra] \\
              loteOrigem = indicesLotes[localOrigem.quadra, localOrigem.lote] \\
              quadraDestino = indicesQuadras[localDestino.quadra] \\
              loteDestino = indicesLotes[localDestino.quadra, localDestino.lote] \\
              adiciona(loteOrigem, rota) \\
              adiciona(quadraOrigem, rota) \\
              \Para{cada rua $r$ em ruasAdulto[k++]} {
                    adiciona(indicesLotes["0000", r.lote], rota) \\
              }
              adiciona(loteDestino, rota) \\
              adiciona(quadraDestino, rota) \\
              adiciona(rota, rotasAdulto) \\
	      }
	}
	
  \Comentario{Criação das rotas para idosos} \\
	rotasIdoso = lista\_vazia() \\
	k = 0 \\
	\Para{$i$ de $0$ ate tamanho(trajetosIdoso)} {
	      \Para{$j$ de $0$ ate tamanho(trajetosIdoso[$i$]) $-1$} {
              rota = lista\_vazia() \\
              localOrigem = trajetosIdoso[$i$, $j$] \\
              localDestino = trajetosIdoso[$i$, $j+1$] \\
              quadraOrigem = indicesQuadras[localOrigem.quadra] \\
              loteOrigem = indicesLotes[localOrigem.quadra, localOrigem.lote] \\
              quadraDestino = indicesQuadras[localDestino.quadra] \\
              loteDestino = indicesLotes[localDestino.quadra, localDestino.lote] \\
              adiciona(loteOrigem, rota) \\
              adiciona(quadraOrigem, rota) \\
              \Para{cada rua $r$ em ruasIdoso[k++]} {
                    adiciona(indicesLotes["0000", r.lote], rota) \\
              }
              adiciona(loteDestino, rota) \\
              adiciona(quadraDestino, rota) \\
              adiciona(rota, rotasIdoso) \\
	      }
	}
}
