\Inicio{  
  \Comentario{Escrita dos índices para fronteiras} \\
	\Para{cada elemento $e$ em indexFronteiras} {
	    escreve($e$, arquivo) \\
	    escreve(";", arquivo) \\
	}
	escreve(novaLinha, arquivo) \\
  
  \Comentario{Escrita do vetor de fronteiras} \\
	\Para{cada elemento $e$ em vetorFronteiras} {
	    escreve($e$, arquivo) \\
	    escreve(";", arquivo) \\
	}
	escreve(novaLinha, arquivo) \\
  
  \Comentario{Escrita dos índices para esquinas} \\
	\Para{cada elemento $e$ em indexEsquinas} {
	    escreve($e$, arquivo) \\
	    escreve(";", arquivo) \\
	}
	escreve(novaLinha, arquivo) \\
  
  \Comentario{Escrita do vetor de esquinas} \\
	\Para{cada elemento $e$ em vetorEsquinas} {
	    escreve($e$, arquivo) \\
	    escreve(";", arquivo) \\
	}
  escreve(novaLinha, arquivo) \\
  
  \Comentario{Escrita dos índices para vértices centros de esquinas} \\
	\Para{cada elemento $e$ em indexCentrosEsquinas} {
	    escreve($e$, arquivo) \\
	    escreve(";", arquivo) \\
	}
	escreve(novaLinha, arquivo) \\
  
  \Comentario{Escrita do vetor de vértices centros de esquinas} \\
	\Para{cada elemento $e$ em vetorCentrosEsquinas} {
	    escreve($e$, arquivo) \\
	    escreve(";", arquivo) \\
	}
  escreve(novaLinha, arquivo) \\
}
